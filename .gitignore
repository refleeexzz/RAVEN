# Binaries
bin/
*.exe
*.out

# Test artifacts
coverage.out
coverage.html

# Data dirs (broker segments, local dev)
data/

# Tools (protoc etc, downloaded locally)
tools/

# Node / frontend
node_modules/
web/console/dist/

# Env / secrets
.env
*.local

# Real k8s secrets — never commit. secrets.example.yaml is the template;
# copy it OUTSIDE the repo (or to secrets.yaml) and fill in real values.
secrets.yaml
*.secrets.yaml
deployments/kubernetes/secrets.yaml

# Owner's private spec (not part of the repo)
RAVEN -- Especificação Técnica.md
RAVEN_IMPLEMENTATION_ROADMAP.md

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
RAVEN_IMPLEMENTATION_ROADMAP.md
